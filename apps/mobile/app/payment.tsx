import { View } from "react-native";

// Payment screen is native-only (requires Stripe dev build).
// This stub satisfies the web bundler.
export default function PaymentScreen() {
  return <View />;
}
