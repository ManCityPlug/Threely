# Hoist all packages to a flat node_modules structure.
# This is required for Metro bundler (Expo) to resolve modules correctly,
# since Metro doesn't understand pnpm's .pnpm virtual store symlink layout.
shamefully-hoist=true
public-hoist-pattern[]=*
